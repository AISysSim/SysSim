# ── Core ─────────────────────────────────────────────────────────────────────
# PyTorch with CUDA — install from the appropriate wheel index for your CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.6
numpy

# ── Profiler (efficiency model training) ─────────────────────────────────────
pandas
scikit-learn
xgboost

# ── Tenstorrent profiling (install on the TT host, not on the trainer host) ──
# Pinned >=0.69.0: 0.65.x slim-LTO firmware objects ICE the bundled sfpi gcc.
# ttnn>=0.69.0

# ── Examples ─────────────────────────────────────────────────────────────────
transformers
megatron-core
