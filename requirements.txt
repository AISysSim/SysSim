# ── Core ─────────────────────────────────────────────────────────────────────
# PyTorch with CUDA — install from the appropriate wheel index for your CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.6
numpy
pandas

# ── Profiler (efficiency model training) ─────────────────────────────────────
scikit-learn
xgboost
flashinfer-python>=0.6  # FP8/FP4 GEMM and attention kernels (Blackwell)

# ── Examples ─────────────────────────────────────────────────────────────────
transformers
megatron-core
